# Codex Tasks

Use this folder for Codex-specific task notes, scratch plans, and review checklists that should stay out of the R package build.

Good first tasks:

- Write a detailed dashboard shell implementation plan.
- Compare `shinydashboard` and `bslib` layout APIs.
- Design the theme token API.
- Implement and test one component at a time.
