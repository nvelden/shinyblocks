% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{block_style}
\alias{block_style}
\title{Select a visual style profile}
\usage{
block_style(profile = "default", ..., scope = NULL)
}
\arguments{
\item{profile}{Built-in style profile name. Defaults to \code{"default"}, which
preserves the current shinyblocks visuals.}

\item{...}{Named token overrides from the curated allowlist, such as
\code{control_height = "2.5rem"}. Override values win over the profile's
built-in values.}

\item{scope}{Optional CSS selector. When supplied, overrides apply only to
elements matching \code{scope} (and the runtime/portal roots inside it) instead
of the whole page. Defaults to \code{NULL} (page-wide).}
}
\value{
A \code{shinyblocks_style} object consumed by \code{\link[=block_page]{block_page()}}.
}
\description{
Selects a built-in visual style profile and, optionally, layers explicit
overrides on top of it. A \emph{style profile} controls visual feel — control
sizing, spacing, surface and overlay metrics, elevation, focus and disabled
treatment, and motion — through a curated set of \verb{--sb-*} custom properties.
This is separate from \code{\link[=block_theme]{block_theme()}}, which owns semantic light/dark
\strong{colour} tokens.
}
\details{
Pass the result to \code{\link[=block_page]{block_page()}} via its \code{style} argument. The profile is
applied page-wide: \code{block_page()} places \code{data-sb-style="<profile>"} on the
\code{.sb-app} shell, so profile tokens also reach dialog, popover, tooltip, and
select portal content (the portal root lives inside \code{.sb-app}).

Overrides use ergonomic snake-case names from a fixed allowlist (for example
\code{control_height}, \code{surface_padding}, \code{focus_ring_width}); see
\code{\link[=block_style_profiles]{block_style_profiles()}} for available profiles. Raw \verb{--sb-*} CSS-variable
names are intentionally not accepted.
}
\examples{
block_style("default")
block_style("default", control_height = "2.5rem", surface_gap = "2rem")
}
\seealso{
Other theme: 
\code{\link{block_dark_mode_toggle}()},
\code{\link{block_style_profiles}()},
\code{\link{block_theme}()},
\code{\link{block_theme_presets}()},
\code{\link{update_block_theme}()}
}
\concept{theme}
