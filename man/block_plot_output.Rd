% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{block_plot_output}
\alias{block_plot_output}
\title{Frame a reactive plot output}
\usage{
block_plot_output(
  id,
  width = "100\%",
  height = NULL,
  aspect = NULL,
  border = FALSE,
  rounded = TRUE,
  caption = NULL,
  click = NULL,
  dblclick = NULL,
  hover = NULL,
  brush = NULL,
  inline = FALSE,
  fill = !inline,
  class = NULL,
  style = NULL
)
}
\arguments{
\item{id}{Shiny output id, passed verbatim to \code{\link[shiny:plotOutput]{shiny::plotOutput()}}.}

\item{width, height}{CSS lengths forwarded to the Shiny output. \code{height = NULL} resolves to \code{"100\%"} when \code{aspect} is set, otherwise Shiny's default.}

\item{aspect}{Aspect ratio for the media box: \code{NULL}, a positive number, or a
\code{"w/h"} string (e.g. \code{"16/9"}).}

\item{border}{Draw a border around the media box.}

\item{rounded}{Round the media box corners (and clip overflow).}

\item{caption}{Optional \verb{<figcaption>} text shown below the plot.}

\item{click, dblclick, hover, brush}{Forwarded to the Shiny output unchanged.}

\item{inline, fill}{Forwarded to the Shiny output. \code{fill} defaults to
\code{!inline} to match \code{\link[shiny:plotOutput]{shiny::plotOutput()}}.}

\item{class}{Additional classes for the \verb{<figure>} wrapper.}

\item{style}{Inline style for the \verb{<figure>} wrapper.}
}
\value{
An \code{htmltools} \verb{<figure>} tag.
}
\description{
Wraps \code{\link[shiny:plotOutput]{shiny::plotOutput()}} in a shadcn-styled frame (aspect box, border,
radius, optional caption). App-author server code stays vanilla Shiny:
\code{output$id <- shiny::renderPlot(...)} is unchanged. Covers base graphics,
ggplot2, and lattice. The plot's accessible name (\code{alt}) is server-controlled
via \code{\link[shiny:renderPlot]{shiny::renderPlot()}}'s \code{alt}; the frame cannot set it.
}
\details{
Unlike \code{\link[=block_image_output]{block_image_output()}} there is no \code{fit} argument: \code{\link[shiny:renderPlot]{shiny::renderPlot()}}
already renders to the media box size, so \code{object-fit} would have no visible
effect.
}
\seealso{
Other outputs:
\code{\link[=block_image_output]{block_image_output()}}
}
\concept{outputs}
