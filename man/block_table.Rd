% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{block_table}
\alias{block_table}
\title{Render a data frame as a styled table}
\usage{
block_table(
  data,
  columns = NULL,
  caption = NULL,
  max_rows = NULL,
  na = "",
  digits = NULL,
  rownames = FALSE,
  row_format = NULL,
  striped = FALSE,
  hover = TRUE,
  bordered = FALSE,
  selection = c("none", "single", "multiple"),
  selected = NULL,
  id = NULL,
  class = NULL,
  style = NULL
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{columns}{Optional named list of per-column overrides created with
\code{table_column()}. Names must match columns in \code{data}.}

\item{caption}{Optional caption rendered below the table.}

\item{max_rows}{Optional non-negative integer limiting the number of rendered
rows. When rows are truncated, a footer note reports the total row count.}

\item{na}{String used to render missing values. Defaults to \code{""} (empty
cell). Per-column overrides win via \code{table_column(na = )}.}

\item{digits}{Optional non-negative integer giving the number of decimal
places for default numeric formatting. \code{NULL} keeps R's default \code{format()}.
Ignored for columns with a custom \code{format} function. Per-column overrides
win via \code{table_column(digits = )}.}

\item{rownames}{Whether to render \code{row.names(data)} as a leading column.}

\item{row_format}{Optional \verb{function(row, i)} called once per rendered row,
where \code{row} is the original (unformatted) row as a named list and \code{i} is the
row index. Return \code{NULL} for no styling, or a list with optional \code{intent},
\code{emphasis}, \code{class}, and/or \code{style} entries applied to that row's \verb{<tr>}.}

\item{striped}{Whether to zebra-stripe body rows.}

\item{hover}{Whether rows highlight on hover. Defaults to \code{TRUE} (shadcn base).}

\item{bordered}{Whether to draw cell borders.}

\item{selection}{Row-selection mode, following the DT idiom. One of \code{"none"}
(default; the table is presentational and reports no value), \code{"single"}
(one row selectable at a time), or \code{"multiple"} (any number of rows). When
enabled the table reports its selection to the server (see \emph{Selection}).}

\item{selected}{Optional integer vector of 1-based row indices to select on
load. Only valid when \code{selection} is \code{"single"} or \code{"multiple"}.}

\item{id}{Optional input id. Required if the table is updated from the
server with \code{\link[=update_block_table]{update_block_table()}} or uses row \code{selection}; static,
non-selectable tables can omit it.}

\item{class}{Additional classes on the runtime mount.}

\item{style}{Optional inline custom styles.}
}
\value{
An \code{htmltools} tag.
}
\description{
\code{block_table()} renders a data frame or tibble as a runtime-owned shadcn
table. Cell formatting happens in R before the payload is sent to the
browser. The same formatting pipeline is reused by \code{\link[=update_block_table]{update_block_table()}},
so a server-side refresh produces an identical payload.
}
\section{Selection}{

With \code{selection} set to \code{"single"} or \code{"multiple"}, rows become clickable and
the table reports its selection through Shiny inputs, mirroring the DT
package so existing DT code ports over:
\itemize{
\item \verb{input$<id>} and \verb{input$<id>_rows_selected} -- integer vector of the
selected 1-based row indices (the bare id is a shinyblocks convenience;
\verb{_rows_selected} is the DT-compatible name).
\item \verb{input$<id>_row_last_clicked} -- the 1-based index of the most
recently clicked row.
\item \verb{input$<id>_cell_clicked} -- a list with \code{row} (1-based), \code{col}
(1-based rendered column index), and \code{value} (the displayed cell text)
for the most recent click.
}
Push a selection from the server with \code{update_block_table(selected = )}.
}

\seealso{
Other content: 
\code{\link{block_alert}()},
\code{\link{block_alert_action}()},
\code{\link{block_alert_description}()},
\code{\link{block_alert_title}()},
\code{\link{block_badge}()},
\code{\link{block_card}()},
\code{\link{block_card_content}()},
\code{\link{block_card_description}()},
\code{\link{block_card_footer}()},
\code{\link{block_card_header}()},
\code{\link{block_card_title}()},
\code{\link{block_code}()},
\code{\link{block_dialog}()},
\code{\link{block_empty}()},
\code{\link{block_popover}()},
\code{\link{block_separator}()},
\code{\link{block_skeleton}()},
\code{\link{block_spinner}()},
\code{\link{block_tooltip}()},
\code{\link{block_value_box}()},
\code{\link{table_column}()},
\code{\link{update_block_dialog}()},
\code{\link{update_block_popover}()},
\code{\link{update_block_table}()}
}
\concept{content}
